# Day 04 - More Packages, Key Pair, Service

##  Ansible - Install Multiple Packages

---
- name: Install packages for pathnex
  hosts: all
  become: yes

  tasks:
    - name: Intall tools
      yum:
        name:
          - git
          - wget
        state: present


## Terraform - EC@ with key pair (c6i.8xlarge)

provider "aws" {
  region = "us-east-1"
}

resource "aws_key_pair" "sumit-pathnex" {
  key_name   = "sumit-pathnex"
  public_key = ""
}

resource "aws_instace" "sumit-pathnexkey" {
  ami           = "ami-..."
  instane_type  = "c6i.xlarge"
  Key_name      = "aws_key_pair"
  
  tags = {
    Name = "sumit-pathnexEC2"
  }
}


## Kubernetes - Create Clusterip service

apiVersion: v1
kind: service
metadata:
  name: sumit-pathnex-service
spec:
  selector:
    app: sumit-pathnex-app
  ports:
    - port: 80
    targetPort: 8080


# Parallel Builds
## Jenkins pipeline - parallel Build & Test


pipeline {
  agent any
  stages {
      stage('Parallel Tasks' {
          parallel {
              stage('Build') {
                  steps {
                      sh 'mvn clean compile'
                  }
              }
              stage('Test') {
                  steps {
                      sh 'mvn test'
                  }
              }
          }
      }
  }
}

## GItLab CI - Parallel jobs


stages:
  - build-test

maven-build:
  stage: build-test
  image: maven:3.8.1-jdk-17
  script:
    - git clone https://github.com/Pathnex/sample-java-app.git
    - cd sample-java-app
    -mvn clean compile
  parallel: 1

maven-test:
  stage: build-test
  image: maven:3.8.1-jdk-17
  script:
    - cd sample-java-app
    - mvn test
  parallel: 1
